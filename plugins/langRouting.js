export default (context, inject, locale) => {
    /*const langButton = (msg) => console.log(`Hello ${msg}!`)
    // Inject $hello(msg) in Vue, context and store.
    inject('hello', langButton)*/
    console.log('hej')
}